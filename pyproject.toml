[project]
name = "docling-api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "docling>=2.4.0",
    "fastapi[standard]>=0.115.4",
    "pypdf>=5.1.0",
]
